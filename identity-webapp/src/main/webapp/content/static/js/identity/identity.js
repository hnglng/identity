

define('identity', ['jquery'], function($) {

    "use strict";

    var identity = {};
    identity.Model = {};
    identity.View = {};
    identity.Controller = {};

    identity.init = function () {

    };

    window.Identity = identity;
    return identity;

});
